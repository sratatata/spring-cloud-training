spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://localhost:8001
      label: 12-Dynamic-configuration
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false