spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://localhost:8001
      label: 10-Docker
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false