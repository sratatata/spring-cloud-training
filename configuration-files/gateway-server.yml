zuul:
  prefix: /api-v1
  host:
    socket-timeout-millis: 20000
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
ribbon:
  ReadTimeout: 100000
  ConnectTimeout: 100000